{
  "name": "wattim-extension",
  "version": "1.0.0",
  "description": "Mindful digital detox & conscious pause browser extension",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node tests/run-all.mjs",
    "lint": "npx web-ext lint --source-dir extension",
    "build:firefox": "npx web-ext build --source-dir extension --overwrite-dest"
  }
}
